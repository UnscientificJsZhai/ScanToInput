<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ScanToInput" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.ScanToInput.IME" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- IME specific configurations can go here -->
    </style>
</resources>